# Eclipse stuff
.project
.pydevproject
.settings

# For the test output, we check in the .golden files but not the .yaml generated by the tests
multicluster/pkg/test/istio-expose-binding/*.yaml
multicluster/pkg/test/generated/*.yaml

# Binaries we build
multicluster/cmd/mc-tool/mc-tool
multicluster/build/docker/mcagent

# Logs we generate
scripts/clusters/*.log

# Emacs temp files
*~
