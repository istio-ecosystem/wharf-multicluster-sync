# Expose the "ratings" service
apiVersion: v1
kind: Service
metadata:
  name: ratings
  labels:
    app: ratings
  annotations:
    multicluster.istio.io/acceptor-clusters: "acceptor-cluster"
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: ratings
